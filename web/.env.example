# Production: public API origin (backend must allow CORS for your web site).
# VITE_API_BASE_URL=https://api.example.com

# Optional: dev proxy target (default http://localhost:3000)
# VITE_PROXY_TARGET=http://localhost:3000
