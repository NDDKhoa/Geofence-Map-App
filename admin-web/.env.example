# Sao chep thanh .env.development neu can.
# De trong: admin-web goi /api/... cung origin, Vite proxy (vite.config.js) chuyen toi backend.
# Dat day: goi truc tiep backend (can CORS cho origin admin).
# VITE_API_BASE=http://localhost:3000
