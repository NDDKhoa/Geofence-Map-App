PORT=3000
MONGO_URI=mongodb://127.0.0.1:27017/vngo_travel
JWT_SECRET=your_jwt_secret_here
CORS_ORIGIN=http://localhost:3000,http://localhost:5173,http://localhost:5174

# QR scan URL base (no trailing slash). Mobile app + printed QR must use the same host/path.
SCAN_QR_URL_BASE=https://thuyetminh.netlify.app/app/scan
QR_SCAN_TOKEN_EXPIRES_IN=24h
